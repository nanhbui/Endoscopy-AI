# CV pipeline
opencv-python>=4.8.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0

# Multi-object tracker (StrongSORT via boxmot) — emits stable per-lesion
# track_id consumed by FE "Xác nhận luôn" / "Bỏ qua" UX. Without this the
# worker falls back to no tracker and track_id stays 0 for every detection.
boxmot>=10.0.0

# Voice recognition — faster-whisper (nhẹ hơn openai-whisper, không cần torch)
faster-whisper>=1.0.0

# LLM & Embeddings
openai>=1.0.0
tiktoken>=0.5.0
# KB RAG (Phase 2) embeddings are served by Ollama (/api/embeddings, bge-m3) via
# httpx — see embeddings.py. sentence-transformers was dropped: its transformers
# dep pins regex>=2025 which conflicts with boxmot's regex<2025 in one process.

# LangChain / LangGraph — RAG workflow
langchain>=0.1.0
langchain-openai>=0.0.2
langchain-community>=0.0.10
langchain-core>=0.1.0
langgraph>=0.0.26
langsmith>=0.1.0

# Vector DB (RAG + negative pattern store)
chromadb>=0.4.0
faiss-cpu>=1.7.4  # dùng cho Negative Pattern DB (Idea 1)

# Database
pymongo>=4.0.0

# API server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Logging
loguru>=0.7.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.9
requests>=2.31.0
tqdm>=4.66.0
psutil>=5.9.0
